# 🛡️ E-CORP SECURITY TERMINAL

**Інтерактивна гра-симулятор пентесту з подвійним інтерфейсом та інтегрованим AI.**
*Розроблено в рамках IT-марафону.*

   

> **⚠️ DISCLAIMER / ВІДМОВА ВІД ВІДПОВІДАЛЬНОСТІ**
> Цей проєкт є **навчальною грою**. Усі "злами", "експлойти" та "атаки" симулюються виключно в JavaScript середовищі браузера. Жодних реальних мережевих атак не відбувається. Дані (IP, User-Agent) відображаються лише користувачеві для демонстрації роботи систем моніторингу.

-----

## 📋 Про проєкт

Це веб-гра у жанрі **CTF (Capture The Flag)**, яка ламає "четверту стіну". Гравець виступає у ролі етичного хакера, який має знайти вразливості в системі E-CORP.

**Особливість:** Інтерфейс розділено на дві частини:

1.  **Ліве вікно (Attack Terminal):** Ваша консоль, де ви проводите аудит.
2.  **Праве вікно (Defense Monitor):** Симуляція системи безпеки, яка "бачить" вас (показує ваш реальний IP, браузер та спроби атак).

### 🔑 Ключові можливості:

  * **Dual View:** Унікальний спліт-скрін інтерфейс (Атака vs Захист).
  * **Realism:** Використання JavaScript-консолі (F12) як основного контролера.
  * **Security Monitor:** Скрипт в реальному часі аналізує ваше середовище (Local Storage, Extensions, IP) і виводить це на екран "захисту" для занурення.
  * **OWASP Top 10:** Рівні базуються на реальних вразливостях (SQL Injection, XSS, IDOR, LFI).
  * **AI Co-Pilot:** Інтеграція з **Google Gemini API**. ШІ знає контекст поточного рівня і дає підказки.

-----

## 🚀 Як запустити (Інструкція)

Цей проєкт не потребує встановлення Node.js або складних залежностей. Це чистий SPA (Single Page Application).

### Крок 1: Запуск

1.  Завантажте файл `index.html`.
2.  Відкрийте його у будь-якому сучасному браузері (Chrome, Edge, Firefox, Brave).
3.  Ви побачите інтерфейс з двома терміналами.

### Крок 2: Початок гри

1.  Натисніть клавішу **`F12`** (або `Ctrl+Shift+I`), щоб відкрити Інструменти розробника.
2.  Перейдіть у вкладку **Console**.
3.  Введіть команду для старту:
    ```javascript
    sys.init()
    ```

-----

## 🎮 Доступні команди

Всі взаємодії відбуваються через об'єкт `sys` у консолі:

  * `sys.help()` — Показати список усіх інструментів.
  * `sys.hint()` — Отримати статичну підказку.
  * `sys.sqlmap('payload')` — Тест вразливості SQL Injection (Рівень 1).
  * `sys.beef('payload')` — Тест вразливості XSS (Рівень 2).
  * `sys.burp(id)` — Маніпуляція запитами IDOR (Рівень 3).
  * `sys.msf('file')` — Читання системних файлів LFI (Рівень 4).

-----

## 🤖 Налаштування AI (Gemini)

Щоб використовувати ШІ-помічника (команда `sys.ask`), вам потрібен безкоштовний API ключ від Google.

### Як отримати ключ (1 хв):

1.  Перейдіть на [Google AI Studio](https://aistudio.google.com/app/apikey).
2.  Натисніть **"Create API Key"**.
3.  Скопіюйте отриманий ключ (починається з `AIza...`).

### Як активувати в грі:

У консолі браузера введіть наступну команду:

```javascript
sys.config_ai('ВСТАВТЕ_ВАШ_КЛЮЧ_ТУТ')
```

### 🔐 БЕЗПЕКА ТА КОНФІДЕНЦІЙНІСТЬ:

Ключ зберігається **локально** у вашому браузері (LocalStorage). Він нікуди не передається, окрім як напряму до Google API для генерації відповіді. "Система безпеки" у правому вікні лише помітить, що ви додали ключ, для створення ігрової атмосфери.
